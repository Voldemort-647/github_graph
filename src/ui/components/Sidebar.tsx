"use client";

// Sidebar is reserved for future panels (filter controls, history, etc.)
// Currently the NodeDetail panel fills this role when a node is selected.

export default function Sidebar() {
  return null;
}
